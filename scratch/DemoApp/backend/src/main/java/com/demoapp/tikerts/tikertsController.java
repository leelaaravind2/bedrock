package com.demoapp.tikerts;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * DEVELOPER-OWNED — created once by Thraksha, then NEVER regenerated.
 *
 * Add custom endpoints for tikerts here. The standard CRUD lives in
 * tikertsControllerBase (Thraksha-owned). This class is safe to edit;
 * regeneration will not touch it.
 */
@RestController
@RequestMapping("/api/tikertss")
public class tikertsController extends tikertsControllerBase {
    // Your custom endpoints go here.
}
