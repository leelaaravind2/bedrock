package com.demoapp.tikerts;

import jakarta.persistence.Entity;
import jakarta.persistence.Table;

/**
 * DEVELOPER-OWNED — created once by Thraksha, then NEVER regenerated.
 *
 * Add your domain behaviour for tikerts here. The generated field
 * mapping lives in tikertsBase (Thraksha-owned). This class is safe
 * to edit; regeneration will not touch it.
 */
@Entity
@Table(name = "tikertss")
public class tikerts extends tikertsBase {
    // Your business logic goes here.
}
