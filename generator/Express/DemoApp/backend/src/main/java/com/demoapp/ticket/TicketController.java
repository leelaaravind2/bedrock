package com.demoapp.ticket;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * DEVELOPER-OWNED — created once by Thraksha, then NEVER regenerated.
 *
 * Add custom endpoints for Ticket here. The standard CRUD lives in
 * TicketControllerBase (Thraksha-owned). This class is safe to edit;
 * regeneration will not touch it.
 */
@RestController
@RequestMapping("/api/tickets")
public class TicketController extends TicketControllerBase {
    // Your custom endpoints go here.
}
